package org.oxygen.redio.gui.containers

import net.minecraft.tileentity.TileEntity

class ContainerSetName(override val tileEntity: TileEntity) extends ContainerBase(tileEntity)
{
	/* nothing else */
}
