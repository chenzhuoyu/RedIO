package org.oxygen.redio.gui.containers

import net.minecraft.tileentity.TileEntity

class ContainerEditSource(override val tileEntity: TileEntity) extends ContainerBase(tileEntity)
{
	/* nothing else */
}
